# router

Enrutado isomórfico (Context/Router/HandlerFunc) idéntico en nativo y edge/wasm, más el contrato APIModule para que un módulo publique su API montándose en un router. Solo define la forma; no implementa ningún servidor concreto.
